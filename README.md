# File Indexing Script
This PowerShell script enumerates files in a root folder, extracts dates and references from filenames, and exports CSV indexes.

## Usage
1. Clone this repo.
2. Edit `IndexFiles.ps1` to set `$RootPath` and `$OutDir`.
3. Run in PowerShell:
   ```powershell
   .\IndexFiles.ps1
